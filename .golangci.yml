run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused

issues:
  exclude-rules:
    # Test files: allow unchecked errors in test setup/assertions
    - path: _test\.go
      linters:
        - errcheck
